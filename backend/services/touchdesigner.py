class TouchDesignerClient:
    def set_scene(self, installation_name: str, scene_name: str):
        print(
            f"[TouchDesigner] Changing {installation_name} to scene {scene_name}"
        )